name: Release Hub (Staging)

permissions:
  contents: write

on:
  push:
    tags:
      - 'hub-[0-9]+.[0-9]+.[0-9]+-*'

jobs:
  deploy:
    uses: ./.github/workflows/deploy-hub.yml
    with:
      environment: Staging
      wrangler-env: staging
      pre-release: true
    secrets: inherit
