name: Release Hub (Production)

permissions:
  contents: write

on:
  push:
    tags:
      - 'hub-[0-9]+.[0-9]+.[0-9]+'

jobs:
  deploy:
    uses: ./.github/workflows/deploy-hub.yml
    with:
      environment: Production
    secrets: inherit
